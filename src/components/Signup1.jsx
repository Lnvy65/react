export default function Signup1() {
    return(
        <>
            <form onSubmit={() => alert('Submitting!')}>
                <input />
                <button type="submit">Submit</button>
            </form>
        </>
    )
}